anthropic>=0.96.0
httpx  # direct dep: Gemini eval backend in shared/providers.py
google-auth  # OAuth for the Vertex AI eval backend (VERTEX_PROJECT in .env)
claude-agent-sdk>=0.2.0  # backend: claude_code — also requires the Claude Code CLI
pandas
pyyaml
ruamel.yaml>=0.18  # comment-preserving round-trip for the viewer axes editor
python-dotenv
tqdm
jsonlines
tenacity
streamlit>=1.40
faker  # seeded fictional people/org name pools (shared/entity_pools.py)
numpy  # embeddings math (shared/embeddings.py, evals/diversity.py)
openai>=1.40  # embeddings for the semantic diversity eval (shared/embeddings.py)

# Test-only (inert outside pytest runs)
pytest>=8.0
pytest-socket>=0.7
