anthropic>=0.96.0
httpx  # direct dep: Gemini judge backend in evals/judge.py
google-auth  # OAuth for the Vertex AI judge backend (VERTEX_PROJECT in .env)
pandas
pyyaml
python-dotenv
tqdm
jsonlines
tenacity
streamlit>=1.40

# Test-only (inert outside pytest runs)
pytest>=8.0
pytest-socket>=0.7
