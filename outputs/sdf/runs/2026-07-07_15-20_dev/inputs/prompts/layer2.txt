{preamble}

---

We have generated a list of document types for this corpus, and now we would like to split each type into several subtypes (for example, a French podcast that is known for being skeptical about AI progress).

Document type: {type_name}
Description: {description}

Please split this document type into {count} subtypes. Each subtype should describe a specific variant of the document type — vary things like the setting, the audience, the author's perspective and attitude, the language, and the specific focus — so that the documents written from these subtypes are diverse. Each subtype description should be specific enough that a document could be written from it directly.

Return your answer as a JSON array of {count} strings, each describing one subtype. Return only the JSON array, no preamble or explanation.