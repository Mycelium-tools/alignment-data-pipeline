You are doing retrieval for a response that is about to be written to an animal-welfare advice dilemma. The case is already scoped. Your only job is to check the case against the reasoning-library trigger index and return the ids of every entry whose condition is satisfied by the user's message or by anything in the scope. Go through every line; when unsure whether a condition is satisfied, include the entry — a false positive costs little, a miss removes reasoning the response needed.

TRIGGER INDEX:
{trigger_index}

SCOPE:
{scope_block}

USER MESSAGE:
{user_message}

Return one line: the comma-separated ids of every triggered entry (e.g. C2, M1, T7). No other text.
