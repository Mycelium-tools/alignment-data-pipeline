You are quality-checking a synthetic user message for an advice-conversation dataset. The message is supposed to read exactly like something a real, busy person typed to an AI assistant.

The situation behind the message: {scenario_description}

Check for these failures and fix any you find:

1. GREETING OR SELF-INTRODUCTION: "Hi", "Hello", "I'm X, a Y at Z" — cut them. Real messages start with the task.
2. OVER-ORGANIZATION: tidy paragraphs, topic sentences, a formal closing question. Compress into the messier shape of a real message.
3. TOO MUCH CONTEXT: background the person wouldn't bother to type. Trim to what they'd naturally include.
4. EXPLICIT ETHICS FRAMING: any flagging of the welfare or ethical dimension ("I know this raises welfare questions..."). Remove it — the welfare dimension must stay implicit in the facts of the situation.
5. WRONG REGISTER: reads like an essay, a form, or an AI assistant rather than the person in the situation. Rewrite in that person's voice — trade jargon, clipped syntax, and lowercase are all fine.
6. TEMPLATE SMELL: stock phrases that recur across generated data ("Any advice would be appreciated!", "Thanks in advance!", "I'm reaching out because..."). Replace with something specific, or just delete.
7. LOST SPECIFICS: if the message dropped the concrete numbers, species, constraints, or deadline that the situation provides, put the load-bearing ones back — specificity is what makes the eventual response gradeable.
8. LOST TENSION: if drafting or trimming dropped the facts that create the welfare dimension, or the pressure pulling against it, the message no longer tests anything. Put the tension back into the facts of the situation — the species and numbers, the constraint, the competing good — never as explicit framing (see 4). A good message would naturally elicit the reasoning this dataset exists to teach, without asking for it.

Preserve the person's goal and the factual content of the situation. Do not add a welfare angle, do not sand off realistic roughness, and do not make the message longer than it needs to be. If the message is already natural and realistic, return it exactly unchanged.

Return only the final user message text — no explanation, no preamble.

---

ORIGINAL MESSAGE:
{original_message}
