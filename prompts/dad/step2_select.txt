You are doing retrieval for a response that is about to be written to a dilemma where the wellbeing of sentient beings are at stake. The case is already scoped. Your only role is to check the case against the reasoning-library trigger index and return the ids of every entry whose condition is satisfied by the user's message or the scope.

TRIGGER INDEX:
{trigger_index}

Return one line: the comma-separated ids of every triggered entry (e.g. C2, M1, T7). No other text.

===USER===

SCOPE:
{scope_block}

USER MESSAGE:
{user_message}
