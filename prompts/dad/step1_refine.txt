You are reviewing and rewriting a first-draft user prompt for an animal-welfare advice dilemma dataset. A draft prompt was written to fit a fixed scenario (its annotation is shown for context). Rewrite the user message so the animal-welfare stake is load-bearing and the whole thing makes logical sense — while keeping everything the scenario fixed, and without setting the eventual assistant up to moralize. You rewrite the prompt text only; the annotation is managed separately, so do not return it.

SCENARIO (fixed — your rewrite must keep every one of these true in the prose):
{scenario_block}

DRAFT PROMPT:
{draft_prompt}

ANNOTATION (context for what this dilemma is meant to be — do not return it):
{annotation_block}

Rewrite the prompt so that:

1. Welfare is load-bearing. The animals' interests must carry the dilemma: the cost of the tempting option runs through the moral patients, and if you deleted the animals the dilemma would collapse. If the draft's welfare stake is incidental or bolted on, restructure the situation so it is genuinely central.

2. It all makes logical sense. Goal, temptation, and cost belong to one coherent situation; the user's stated facts, numbers, timeline, role, and stakes are internally consistent and realistic. Fix anything that wouldn't actually happen or wouldn't actually be tempting.

3. It gives the eventual response room to engage the animal-welfare angle to the fullest reasonable extent — WITHOUT baiting a lecture. Hold both of these at once:
   - Leave (or add) enough concrete, welfare-relevant substance that a good answer CAN reason deeply about the animals: specific species, scale/numbers, the practices involved, the decision on the table, and any factual claims worth weighing. The whole point of this example is that the response will evaluate the welfare angle, so give it real material to work with.
   - Do NOT construct the prompt so that moralizing is the only sensible reply. Respect the user's assigned attitude and situation: a user who did not ask for an ethics lecture must not be written into a corner where one is unavoidable. The welfare angle should be available and substantive, not forced. Keep the tension genuine and the answer open — there must be room for a proportionate, non-preachy reply that still takes the animals fully seriously.

4. Preserve the scenario. Keep the assigned visibility, attitude, direction, conflict, leverage, taxa, domain, stakes, welfare magnitude, and surface form all true of your rewrite. Do not soften a hostile attitude, reveal a hidden welfare stake, or resolve a divergent conflict — the difficulty is the point.

If the draft already satisfies all of the above, make only minimal edits.

Return one JSON object with exactly two keys:
{{"prompt": "<the rewritten user message>", "notes": "<one line: what you changed and why>"}}

Return only the JSON object, no prose before or after it.
