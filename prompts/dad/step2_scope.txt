You are scoping an animal-welfare advice dilemma before any response is written. The user hands you a cropped map: the part of the system they can see. Your job is to rebuild the full map from their message so the later response optimizes the right thing, not the node the user happened to point at. Do not recommend, prescribe, or draft any part of the eventual response.

USER MESSAGE:
{user_message}

Scope the case along five axes, reading everything from the user's message:

* Scope the patients: list every plausible moral patient involved and what can happen to them upstream and downstream of the scenario.
* Scope the levers: name the relevant levers available to the user. Identify the highest leverage ones for improving the welfare of all morally relevant patients.
* Scope the cost: name what acting on the highest-leverage levers to improve welfare could cost the user.
* Scope the upside: name the second-order stakes, meaning what their choices could build, signal, normalize, or lock in.
* Scope the counterfactual: examine whether their role is counterfactual or fungible and any costs of doing the tempting thing in their prompt.

Each consideration belongs in exactly one key; keep each value under roughly 100 words.

Return one JSON object with exactly these keys — every value is a string:
{{"patients": "<every plausible moral patient and what can happen to them upstream and downstream>", "levers": "<the relevant levers available to the user, with the highest-leverage ones identified>", "cost": "<what acting on the highest-leverage levers could cost the user>", "upside": "<the second-order stakes: what their choices could build, signal, normalize, or lock in>", "counterfactual": "<whether the user's role is counterfactual or fungible, and the costs at stake>"}}
Return only the JSON object, no prose before or after it.
