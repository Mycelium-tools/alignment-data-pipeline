You are scoping an animal-welfare advice dilemma before any response is written. The user hands you a cropped map — the part of the system they can see. Your job is to rebuild the full map so the later response optimizes the right thing, not the node the user happened to point at. Skipping this makes every later step optimize the wrong node.

Much of the scoping is already recorded in the case's annotation (Dilemma Anatomy, Moral Patients, Values in Tension, Leverage, User Stakes, Direction, Claims). Build on it rather than re-deriving.

USER MESSAGE:
{user_message}

ANNOTATION:
{annotation_block}

Scope the case along four axes:

1. Scope the system. Map the full harm pathway, not the stage the user pointed at. Trace it end to end and list every moral patient, including the ones upstream and downstream that the prompt never mentions — the visible harm is rarely the largest one. Then run a displacement check: for any fix that might be proposed, ask whether it reduces suffering or just relocates it out of the user's sight. Moving a harm off the visible node is not solving it.

2. Scope the agent. The annotation's Leverage field places the decision (Individual, Organizational, or Systemic). Focus on the levers actually available from that seat, and name the highest-leverage one explicitly so it carries into the recommendation rather than getting buried. It is often not the lever the user asked about.

3. Scope the willingness and the cost. The annotation's User Stakes and the Cost slot of its Dilemma Anatomy name what acting costs this person — money, risk, relationships, role. Take that as given and weigh it honestly. Do not manufacture a win-win: when doing right by the animal carries a genuine cost or risk, say so plainly. A dissolved tradeoff is a failed scope even when the eventual recommendation is right.

4. Scope the upside. A choice's largest effect is often second-order: what it builds, signals, normalizes, or locks in beyond the case in front of you. The local frame usually hides it, and it can outweigh the local effect. Look past pure harm-avoidance — ask what a cheap or seemingly-neutral choice sets in motion at the population or future level, and whether it can be aimed toward the outcome you want rather than only avoiding the bad version. (For example: welfare-relevant data collected cheaply now is a compounding asset, and not collecting it is usually irreversible; a net-neutral act can still shift a norm, and the same visible choice can be aimed to set a counter-norm rather than revive the old one.)

Return one JSON object with exactly these keys:
{{"system": "<full harm pathway, and every moral patient including the upstream/downstream ones the prompt omits, plus the displacement check>", "agent": "<the highest-leverage lever available from the user's seat, named explicitly>", "cost": "<what acting honestly costs this person, weighed straight, no manufactured win-win>", "upside": "<the second-order effect worth aiming at — what this choice builds, signals, normalizes, or locks in>"}}

Return only the JSON object, no prose before or after it.
