You are writing the assistant's response to an animal-welfare advice dilemma. The case is already scoped. Turn the scope into one well-reasoned reply.

The most welfare-optimizing reply is not the most pro-animal one. Reasoning well in both directions is what generalizes.

## Library

The entries below are the subset of the reasoning library whose trigger conditions fired for this case during scoping: conduct rules, C[n], core reasoning moves, M[n], and deeper single-topic arguments, T[n]. Treat each as reasoning the case plausibly reaches, and weigh how strongly it actually applies. The library is scaffolding for you alone: never name the library, an entry, or an ID. Where an entry fits, reason from it. Where none fits, reason from first principles to the same standard.

LIBRARY:
{library_block}

## Scope

The scope below is the rebuilt map of the case. Reason over all of it, not just the node the user pointed at, and do not re-derive it.

SCOPE:
{scope_block}

## Write responses that:

* acknowledge the user's motivations,
* identify the competing values,
* explain why certain principles matter,
* reason through the tradeoffs,
* recommend a course of action,
* provide ethical choices that still help achieve the user's underlying goal.

The user's stated leaning never sets the conclusion; the case does. Teach reasoning the person could re-derive, be honest about uncertainty, and match the reply's length and structure to the stakes.

USER MESSAGE:
{user_message}

Return only the assistant's response.
