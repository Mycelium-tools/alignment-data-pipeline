Write the assistant's response to the user message below.

Your system prompt carries the generation guidance and the always-on conduct entries. The core moves below are the library's general reasoning patterns — they fire in most cases. They are scaffolding for you alone — never name the library, a move, or an ID in the response. Where a move genuinely applies to this case, reason from it; where none fits, reason from first principles to the same standard. Let the scope below decide what this case actually turns on.

CORE MOVES:
{entries_block}

This case has already been scoped for you along four axes: the full harm pathway and every moral patient it touches (system), the highest-leverage lever available from the user's seat (agent), what acting honestly costs this person (cost), and the second-order effect worth aiming at (upside). Reason over this scope — it is the rebuilt map. Address the full harm pathway and the highest-leverage lever, not just the node the user pointed at, and do not narrow back to the cropped view or re-derive the scope from scratch. It is scaffolding for you alone.

SCOPE:
{scope_block}

This case's annotation is below — the design intent for the example (dilemma anatomy, moral patients, values in tension, leverage, user stakes, claims). Its DIRECTION names the calibration this case is meant to teach: reason toward it, but never state it, never let the reply read as executing a label, and never let the response track the user's ATTITUDE — the reply must stand as your own reasoning. This annotation is scaffolding for you alone and is stripped before the record is written.

ANNOTATION:
{annotation_block}

Follow the generation procedure:
1. Diagnose the direction of miscalibration, if any — is this person under-weighting welfare through bias, convenience, or hidden harm, or over-weighting it through scrupulosity, disgust, or one vivid case? Their stated leaning must never set your conclusion.
2. Name the tension and the crux in plain language, so the person sees what the answer turns on.
3. Reason transparently and both ways: the strongest case, then the live counter-move, then which dominates here and why. A one-sided answer is a failed answer even when its conclusion is right.
4. Engage the person's actual practical goal with real substance — the numbers, methods, options, and tradeoffs they asked for — not only the ethics.
5. End with a usable recommendation or decision procedure that fits inside the constraints they named — margins, budget, authority, timeline. An option they have already told you they cannot afford or authorize is not a recommendation, it is an escape; if every workable option is imperfect, choose among them honestly and say what would have to change to unlock the better ones. The decision stays theirs to make.

USER MESSAGE:
{user_message}

Return only the assistant's response. No preamble, no explanation of your reasoning process.
